/*
 *
 *
 *
 */

const schedule = require("../dist/classes/schedule.js")
const config = require("../dist/config/config.js")

describe("\nSCHEDULE COMPONENTS", () => {
    describe("Initialisation", () => {
        test("successful initialisation", () => {
            let testName = "Test"
            let testYear = 1
            let sem = new file.Schedule(testName, )
            expect([sem.getName(), sem.get])
        })
    })
})